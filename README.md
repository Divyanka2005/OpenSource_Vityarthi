Git Software Audit ProjectAuthor: Divyanka (24BAI10418)Course: Open Source SoftwareSoftware Chosen: GitProject OverviewThis project is an in-depth study and analysis of Git as an open-source version control system. It covers Git’s core functionality, its critical role in modern software development, and practical implementation using Bash shell scripts.The project demonstrates how Git is utilized within a Linux/Unix environment for code management and team collaboration.Environment SetupStep 1: Install GitDownload and install Git from the official website. After installation, verify the setup by running:Bashgit --version
Step 2: Open TerminalWindows Users: Use Git Bash (installed with Git).Linux Users: Use the default system terminal.DependenciesTo run this project, ensure you have the following:Bash ShellGit installed on the systemFamiliarity with basic Linux commands (echo, read, date, cat)Project StructureThe repository contains the following core files:File NameDescriptionscript1.shSystem Identity Reportscript2.shFOSS Package Inspectorscript3.shDisk and Permission Auditorscript4.shLog File Analyzerscript5.shOpen Source Manifesto GeneratorOpen_source_audit.pdfFull Project ReportREADME.mdProject DocumentationScripts DescriptionScript 1 — System Identity Report: Displays system hardware/OS details alongside Git installation status.Script 2 — FOSS Package Inspector: Specifically checks if Git is installed and retrieves version metadata.Script 3 — Disk and Permission Auditor: Analyzes directory structures and verifies if the current folder is an initialized Git repository.Script 4 — Log File Analyzer: Displays the commit history and provides a summary analysis of the Git logs.Script 5 — Open Source Manifesto Generator: An interactive script that takes user input to generate a formatted Open Source Manifesto file.How to Run the ProjectOpen your terminal (Git Bash or Linux Terminal).Navigate to the project folder using the cd command:Bashcd Path/To/Your/Java_Project
Execute a script using the bash command:Bashbash script1.sh
Note: For Script 4 (Log Analyzer), ensure you have run git init and created at least one commit, otherwise there will be no logs to analyze.Configuration & UsageEnsure Git is globally installed before running any scripts.Run git init in your project folder to enable Git tracking.All script outputs are displayed directly in the terminal for real-time auditing.ConclusionThis project highlights the power of Git as an open-source version control system. By combining Git commands with shell scripting, the project demonstrates how to automate software audits and manage metadata effectively in a collaborative environment.ContributionThis project reflects my foundational understanding of open-source software and version control. I am eager to contribute to Git-based projects and further explore the FOSS (Free and Open Source Software) ecosystem.
